# Development & test dependencies
-r requirements.txt

# SPEC §10 acceptance: pytest tests/ -v → 82/82 pass
pytest>=8.0
pytest-asyncio>=0.23   # httpx.AsyncClient requires async test support
pytest-cov>=5.0        # coverage reports
httpx[http2]>=0.27     # for testing with HTTP/2
